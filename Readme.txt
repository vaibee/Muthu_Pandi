This project is developed by Vaibhav Charan. 

there are severl changes done to this project interms of optimizing it. 
this gives a result of 0.9 accuracy score using the CNN ml model. 

changes made:

1.
the newest version of keras and tensorflow is used in this project 
-> the import cmd are changed according to the new versions of the keras and tensorflow. 
-> the pickle is replaced with the 'joblib' library (WHICH EVENTUALLY IS BETTER IN DUMPING THE DATA FILE(MODEL_TRAINED.JOBLIB) ACCURATILY